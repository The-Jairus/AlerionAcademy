Brand assets — drop the Alerion files here with these exact names:

  logo.png              sidebar + sign-in screen  (light version, transparent, height >= 60px)
  certificate_logo.png  PDF certificate header    (dark version, transparent, height >= 300px)
  favicon.png           browser tab               (square, 64x64)

If a file is missing the platform still works: it falls back to the text wordmark.
